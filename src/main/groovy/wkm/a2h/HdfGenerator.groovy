package wkm.a2h

class HdfGenerator {
    def testResultNode

    /**
     * Generate the Heimdall Data Format (HDF) from XCCDF Test Results
     * @return a map to parse/save as JSON
     */
    def generate() {
        def hdfMap = [:]

        return hdfMap
    }
}
